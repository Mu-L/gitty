# Work log 2026-08-31

## Summary

```
[Gitty 2026-08-31]

· A JSON file has a Format button beside Wrap, re-indenting the document two
  spaces to a level. `json.ts` is a small parser, not
  `JSON.stringify(JSON.parse(text))`, which does not round-trip a file: it
  turns `1e999` into `null`, reorders numeric-looking keys and drops duplicates
· 1 commit, 20 files touched
```

## Why it matters

**Why do this?**

A minified file arrives as one endless line, and colouring a line that runs
off the pane is no help.

**What problem does it solve?**

Reading a stored `.json` meant leaving Gitty for an editor and coming back.

**What does it affect?**

`json.ts` and `paths.ts`; the button is `DiffHeader`'s and the state is the
`preview` flag `FileDoc` already carried, so history remembers the choice.

---

## Commits

- cc97cfd feat(files): a JSON file has a button that re-indents it
