# Work log 2026-08-30

## Summary

```
[Gitty 2026-08-30]

· Ctrl+E opens a file by name from anywhere: every file git would name, not
  the tree on screen, ranked in `quickopen.ts` by a subsequence match placed
  with a dynamic program — a greedy walk spends the first letter on a
  directory and never reaches the file name
· 1 commit, 24 files touched
```

## Why it matters

**Why do this?**

The tree shows one view at a time, and none of them is the whole repository.

**What problem does it solve?**

Opening a known file was navigation — pick a view, expand, scroll — while the
name was in the reader's head the whole time.

**What does it affect?**

`quickopen.ts` is a leaf module tested without a repository; TerminalPane had
to be told to keep the chord from the shell.

---

## Commits

- f6ea714 feat(files): Ctrl+E opens a file by name from anywhere
