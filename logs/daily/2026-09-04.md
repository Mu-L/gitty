# Work log 2026-09-04

## Summary

```
[Gitty 2026-09-04]

· A file's menu opens its page on the hosting site: `remote.ts` now returns
  both prefixes at once (`RemoteBases`), so one resolution answers the commit
  menu and the file row, and a detached HEAD or untracked file gets no item
· A renamed file diffs against the path it came from — a pathspec is applied
  before git pairs the deletion with the addition, so every single-file
  request now carries the previous path, hunk staging included
· 2 commits, 15 files touched
```

## Why it matters

**Why do this?**

A rename read as a whole file added, and the file row had no way to reach the
same remote page the commit row already offered.

**What problem does it solve?**

The diff was wrong where it mattered most quietly — staging a line put the
whole rename back — and reaching a file on GitHub meant leaving the app.

**What does it affect?**

`patch.ts` and its `retargetRename`, `useStaging`, `remote.ts` and the context
menus; `ref/spec/main-process.md` and the manual.

---

## Commits

- 2f41b45 fix(diff): a renamed file diffs against the path it came from
- d1d3a36 feat(files): a file's menu opens its page on the hosting site
