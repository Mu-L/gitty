# Work log 2026-09-03

## Summary

```
[Gitty 2026-09-03]

· A browsed tree names who last committed each file: one backwards
  `git log -z --name-only` answers every path from the first commit that
  names it, rather than a process per row
· A PDF opens as the document, in the viewer Chromium already carries —
  which needs `plugins: true` and `frame-src 'self' blob:`, and the bytes
  travel as a `Uint8Array` rather than a data: URL
· A file stored with Git LFS previews as the file: `lfs.ts` recognises the
  pointer exactly, and `git.ts` reads the object straight out of the store,
  so no git-lfs on the machine is needed
· The daily logs for 2026-08-28 to 2026-09-02, their length now following
  how much each day held
· 4 commits, 33 files touched
```

## Why it matters

**Why do this?**

Three kinds of file answered the wrong way: a tree said nothing about
ownership, a PDF was Chromium's own failure message, and an LFS file was a
broken icon.

**What problem does it solve?**

Browsing a repository should show what is in it, not what git happens to hand
back — a pointer file, a refusal to render, an unattributed list of paths.

**What does it affect?**

`FilesPane`, `FileDoc` and the new `PdfPane`; `lfs.ts` with its own test file;
`ref/spec/file-viewers.md` and `ref/spec/main-process.md`.

---

## Commits

- 84c5bc7 fix(files): a file stored with Git LFS previews as the file
- 57e82c0 feat(files): a PDF opens as the document
- ac1d55b feat(files): a browsed tree says who last committed each file
- 371e43f docs(logs): the daily logs for 2026-08-28 to 2026-09-02
