# Publish to the official npm registry. The user's global .npmrc may point
# npm at a mirror (e.g. npmmirror) for faster installs; that mirror does not
# allow publishing or logging in, so this project pins the official registry.
# Install-time downloads still use the mirror when the global config sets it.
registry=https://registry.npmjs.org/
